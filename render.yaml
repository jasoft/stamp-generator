services:
  - type: web
    name: stamp-generator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python stamp_webapp.py
    envVars:
      - key: PORT
        value: 10000
    plan: free
